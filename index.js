const { INPUTS, MENU_TYPES } = require('./components/constants');

module.exports = {
    Menu: require('./components/menu'),
    MenuItem: require('./components/menuItem'),
    INPUTS,
    MENU_TYPES,
};
